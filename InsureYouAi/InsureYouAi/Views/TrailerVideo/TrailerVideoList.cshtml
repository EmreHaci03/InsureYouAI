@model List<ResultTrailerVideoDto>
@{
    Layout = "/Views/Shared/_AdminLayout.cshtml";
    string[] gradients = new[]
    {
        "linear-gradient(135deg,#6366f1,#8b5cf6)",
        "linear-gradient(135deg,#06b6d4,#3b82f6)",
        "linear-gradient(135deg,#f59e0b,#ef4444)",
        "linear-gradient(135deg,#10b981,#059669)",
        "linear-gradient(135deg,#ec4899,#f43f5e)",
    };
}
@section BreadCrumb {
    <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
        <div class="breadcrumb-title pe-3"><i class="bi bi-play-btn-fill me-1"></i>Tanıtım Videoları</div>
        <div class="ps-3">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0 p-0">
                    <li class="breadcrumb-item">
                        <a href="javascript:;"><i class="bx bx-home-alt"></i></a>
                    </li>
                    <li class="breadcrumb-item">
                        <a asp-action="TrailerVideoList">Tanıtım Videoları</a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">Liste</li>
                </ol>
            </nav>
        </div>
    </div>
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show rounded-3 border-0 shadow-sm" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>@TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="d-flex flex-wrap align-items-center justify-content-between mb-4 gap-3">
    <div>
        <h4 class="fw-bold mb-1">Tanıtım Videoları <span class="badge rounded-pill bg-light text-dark ms-1">@(Model?.Count ?? 0)</span></h4>
        <p class="text-muted mb-0 small">Ana sayfa tanıtım videolarını yönetin</p>
    </div>
    <div class="d-flex align-items-center gap-2">
        <div class="position-relative">
            <i class="bi bi-search position-absolute top-50 translate-middle-y ms-3 text-muted"></i>
            <input class="form-control ps-5 rounded-pill border-0 shadow-sm" type="text" placeholder="Video ara..." id="searchInput" style="min-width:240px;">
        </div>
        <a asp-action="CreateTrailerVideo" class="btn btn-primary rounded-pill px-4 shadow-sm">
            <i class="bi bi-plus-lg me-1"></i>Yeni Video
        </a>
    </div>
</div>

@if (Model != null && Model.Any())
{
    <div class="row g-3" id="videoGrid">
        @for (int i = 0; i < Model.Count; i++)
        {
            var item = Model[i];
            var gradient = gradients[i % gradients.Length];
            var safeTitle = item.Title ?? "";

            <div class="col-xl-4 col-lg-6 video-card-wrap" data-name="@safeTitle.ToLower()">
                <div class="card border-0 shadow-sm h-100 video-card overflow-hidden">
                    <div class="position-relative" style="overflow:hidden;">
                        @if (!string.IsNullOrEmpty(item.CoverImageUrl))
                        {
                            <img src="@item.CoverImageUrl" class="w-100 video-thumb" style="height:170px; object-fit:cover; transition:transform .5s ease;" alt="@safeTitle" />
                        }
                        else
                        {
                            <div class="w-100 d-flex align-items-center justify-content-center" style="height:170px; background:@gradient;">
                                <i class="bi bi-camera-video-fill text-white fs-1 opacity-75"></i>
                            </div>
                        }

                        <div class="position-absolute top-50 start-50 translate-middle" style="pointer-events:none;">
                            <div class="rounded-circle d-flex align-items-center justify-content-center" style="width:52px; height:52px; background:rgba(255,255,255,0.85); backdrop-filter:blur(4px);">
                                <i class="bi bi-play-fill" style="color:#28314b; font-size:24px; margin-left:2px;"></i>
                            </div>
                        </div>

                        <div class="dropdown position-absolute top-0 end-0 mt-2 me-2">
                            <button class="btn btn-sm btn-icon border-0 bg-white shadow-sm rounded-circle" type="button" data-bs-toggle="dropdown">
                                <i class="bi bi-three-dots-vertical"></i>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                                <li>
                                    <a class="dropdown-item" asp-action="UpdateTrailerVideo" asp-route-id="@item.TrailerVideoId">
                                        <i class="bi bi-pencil-fill me-2 text-warning"></i>Düzenle
                                    </a>
                                </li>
                                @if (!string.IsNullOrEmpty(item.VideoUrl))
                                {
                                    <li>
                                        <a class="dropdown-item" href="@item.VideoUrl" target="_blank">
                                            <i class="bi bi-box-arrow-up-right me-2 text-info"></i>Videoyu Aç
                                        </a>
                                    </li>
                                }
                                <li>
                                    <button type="button" class="dropdown-item text-danger btn-delete-video"
                                            data-id="@item.TrailerVideoId" data-name="@safeTitle" data-image="@item.CoverImageUrl" data-gradient="@gradient">
                                        <i class="bi bi-trash-fill me-2"></i>Sil
                                    </button>
                                </li>
                            </ul>
                        </div>
                    </div>

                    <div class="card-body pt-3 d-flex flex-column">
                        <h6 class="fw-semibold mb-3">@item.Title</h6>
                        <div class="d-flex gap-2">
                            <a asp-action="UpdateTrailerVideo" asp-route-id="@item.TrailerVideoId" class="text-decoration-none small fw-semibold flex-grow-1" style="color:#6366f1;">
                                Düzenle <i class="bi bi-arrow-right ms-1"></i>
                            </a>
                            <button type="button" class="btn btn-sm btn-outline-danger rounded-pill px-3 btn-delete-video"
                                    data-id="@item.TrailerVideoId" data-name="@safeTitle" data-image="@item.CoverImageUrl" data-gradient="@gradient">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <div class="card border-0 shadow-sm">
        <div class="card-body text-center py-5">
            <div class="rounded-circle bg-light d-inline-flex align-items-center justify-content-center mb-3" style="width:72px; height:72px;">
                <i class="bi bi-inbox fs-2 text-muted"></i>
            </div>
            <h6 class="fw-semibold">Henüz video eklenmemiş</h6>
            <p class="text-muted small">Başlamak için ilk tanıtım videonuzu oluşturun</p>
            <a asp-action="CreateTrailerVideo" class="btn btn-primary rounded-pill px-4 mt-2">
                <i class="bi bi-plus-lg me-1"></i>İlk Videoyu Ekle
            </a>
        </div>
    </div>
}

<form id="deleteForm" asp-action="DeleteTrailerVideo" method="post" class="d-none">
    @Html.AntiForgeryToken()
    <input type="hidden" name="id" id="deleteVideoId" value="" />
</form>

<!-- Silme Onay Modalı -->
<div class="modal fade" id="deleteConfirmModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow-lg" style="border-radius:20px; overflow:hidden;">
            <div class="modal-body p-0">
                <div class="text-center pt-4 pb-2 px-4">
                    <div class="rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                         style="width:72px; height:72px; background:#fdeaea;">
                        <i class="bi bi-trash3-fill text-danger" style="font-size:28px;"></i>
                    </div>
                    <h5 class="fw-bold mb-1">Videoyu Sil</h5>
                    <p class="text-muted small mb-3">Bu işlem geri alınamaz. Devam etmek istediğinize emin misiniz?</p>
                </div>

                <div class="mx-4 mb-4 rounded-3 bg-light overflow-hidden d-flex align-items-center gap-3" style="border-left:3px solid #dc3545;">
                    <div id="deleteVideoThumbWrap" style="width:64px; height:64px; flex-shrink:0; overflow:hidden;">
                        <img id="deleteVideoThumb" src="" style="width:100%; height:100%; object-fit:cover; display:none;" />
                        <div id="deleteVideoThumbFallback" class="w-100 h-100 d-flex align-items-center justify-content-center">
                            <i class="bi bi-camera-video-fill text-white fs-4 opacity-75"></i>
                        </div>
                    </div>
                    <div class="min-w-0 text-start py-2 pe-3">
                        <h6 id="deleteVideoName" class="fw-semibold mb-0 small text-truncate"></h6>
                    </div>
                </div>

                <div class="d-flex gap-2 px-4 pb-4">
                    <button type="button" class="btn btn-light flex-grow-1 rounded-pill fw-semibold" data-bs-dismiss="modal">
                        Vazgeç
                    </button>
                    <button type="button" id="confirmDeleteVideoBtn" class="btn btn-danger flex-grow-1 rounded-pill fw-semibold shadow-sm">
                        <i class="bi bi-trash3-fill me-1"></i>Evet, Sil
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .video-card {
        transition: all 0.25s ease;
        border-radius: 16px;
    }

        .video-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px rgba(0,0,0,0.08) !important;
        }

            .video-card:hover .video-thumb {
                transform: scale(1.08);
            }

    .min-w-0 {
        min-width: 0;
    }

    #deleteConfirmModal .modal-content {
        animation: modalPopVideo .25s ease;
    }

    @@keyframes modalPopVideo {
        from {
            opacity: 0;
            transform: scale(.92);
        }

        to {
            opacity: 1;
            transform: scale(1);
        }
    }

    #confirmDeleteVideoBtn:hover {
        background: #b02a37;
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function () {

        var searchInput = document.getElementById('searchInput');
        if (searchInput) {
            searchInput.addEventListener('input', function () {
                var term = this.value.toLowerCase();
                document.querySelectorAll('.video-card-wrap').forEach(function (card) {
                    card.style.display = card.dataset.name.includes(term) ? '' : 'none';
                });
            });
        }

        var deleteModalEl = document.getElementById('deleteConfirmModal');
        var deleteModal = new bootstrap.Modal(deleteModalEl);
        var pendingDeleteId = null;

        document.querySelectorAll('.btn-delete-video').forEach(function (btn) {
            btn.addEventListener('click', function () {
                pendingDeleteId = this.dataset.id;

                document.getElementById('deleteVideoName').textContent = this.dataset.name;

                var img = document.getElementById('deleteVideoThumb');
                var fallback = document.getElementById('deleteVideoThumbFallback');
                var imageUrl = this.dataset.image;

                if (imageUrl) {
                    img.src = imageUrl;
                    img.style.display = 'block';
                    fallback.style.display = 'none';
                } else {
                    img.style.display = 'none';
                    fallback.style.display = 'flex';
                    fallback.style.background = this.dataset.gradient;
                }

                deleteModal.show();
            });
        });

        document.getElementById('confirmDeleteVideoBtn').addEventListener('click', function () {
            if (!pendingDeleteId) return;

            var btn = this;
            btn.disabled = true;
            btn.innerHTML = '<span class="spinner-border spinner-border-sm me-1"></span>Siliniyor...';

            document.getElementById('deleteVideoId').value = pendingDeleteId;
            document.getElementById('deleteForm').submit();
        });

    });
</script>
